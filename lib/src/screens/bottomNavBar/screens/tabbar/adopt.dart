import 'package:flutter/material.dart';

class AdoptScreen extends StatelessWidget {
  final int index;
  const AdoptScreen({super.key, required this.index});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
